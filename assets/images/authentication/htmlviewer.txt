<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Popup Component</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      .popup {
        display: none;
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        background: white;
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        width: 300px;
        text-align: center;
      }

      .popup-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
      }

      .popup-header h2 {
        margin: 0;
        font-size: 1.5rem;
        color: #333;
      }

      .popup-header .close-btn {
        cursor: pointer;
        font-size: 1.5rem;
        color: #28a745;
      }

      .popup-content {
        font-size: 1rem;
        color: #555;
      }

      .popup-content .highlight {
        color: #28a745;
        font-weight: bold;
      }

      .popup-footer {
        margin-top: 20px;
        display: flex;
        justify-content: space-between;
      }

      .popup-footer .btn {
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
      }

      .popup-footer .btn-primary {
        background: #28a745;
        color: white;
      }

      .popup-footer .btn-secondary {
        background: transparent;
        color: #555;
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <div class="popup" id="popup">
      <div class="popup-header">
        <h2>Teste Gratuito</h2>
        <span class="close-btn" id="closeBtn">&times;</span>
      </div>
      <div class="popup-content"> Você terá um teste gratuito por CPF, até <span class="highlight">03 doenças</span>. </div>
      <div class="popup-footer">
        <button class="btn btn-primary" id="registerBtn">Cadastrar</button>
        <button class="btn btn-secondary" id="backBtn">Voltar</button>
      </div>
    </div>
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        // Check if the popup has been shown before
        if (!localStorage.getItem('popupShown')) {
          // Show the popup after 5 seconds
          setTimeout(function() {
            document.getElementById('popup').style.display = 'block';
          }, 5000);
        }
        // Close the popup and set local storage
        document.getElementById('closeBtn').addEventListener('click', function() {
          document.getElementById('popup').style.display = 'none';
          localStorage.setItem('popupShown', 'true');
        });
        // Buttons functionality
        document.getElementById('registerBtn').addEventListener('click', function() {
          alert('Cadastrar button clicked!');
          document.getElementById('popup').style.display = 'none';
          localStorage.setItem('popupShown', 'true');
        });
        document.getElementById('backBtn').addEventListener('click', function() {
          document.getElementById('popup').style.display = 'none';
          localStorage.setItem('popupShown', 'true');
        });
      });
    </script>
  </body>
</html>